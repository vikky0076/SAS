{
  "name": "smart-ai-attendance-system",
  "version": "1.0.0",
  "description": "Smart Attendance System - Multi-Role, Trusted Device validation, Dynamic QR Codes",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../client && npm install --legacy-peer-deps",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
